import { JumpaLoaderScreen } from "@/components/ui/jumpa-loader";

/** Shown while a route in this group resolves — including a hard refresh. */
export default function Loading() {
  return <JumpaLoaderScreen />;
}
